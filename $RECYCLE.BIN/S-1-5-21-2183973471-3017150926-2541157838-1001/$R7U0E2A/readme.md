# 🏦 Simulador de Caixa Eletrônico (If-Else)

Projeto simples desenvolvido em **HTML**, **CSS** e **JavaScript**, com o objetivo de praticar o uso de **estruturas condicionais (`if`, `else if`, `else`)** e **manipulação do DOM**.

---

## 🚀 Funcionalidades

- Exibe o **saldo atual** do usuário.
- Permite digitar um **valor de saque**.
- Valida o valor inserido com as seguintes regras:
  - O valor deve ser **maior que zero**.
  - O valor **não pode ser maior que o saldo disponível**.
  - O valor deve ser **múltiplo de 10**.
- Exibe mensagens de **erro** ou **sucesso** com cores diferentes.
- Atualiza o saldo dinamicamente após cada saque.
- Limpa automaticamente o campo de entrada após a operação.

---

## 🧠 Conceitos aplicados

- Estruturas condicionais (`if`, `else if`, `else`)
- Manipulação do DOM (`document.getElementById`)
- Eventos (`addEventListener`)
- Conversão de tipos (`parseFloat`)
- Interpolação de strings (Template Literals)
- Boas práticas de UX (feedback visual e reset do campo)

---

## 🧩 Estrutura do projeto
├── index.html # Estrutura da interface
├── style.css # Estilos visuais
└── script.js # Lógica de validação e interação

> No seu caso, o arquivo JS e CSS estão nomeados como `if-else.js` e `if-else.css`.

---

## 💻 Como executar

1. Faça o download ou clone o repositório:
   ```bash
   git clone https://github.com/seu-usuario/projeto-caixa-eletronico.git

## 🧰 Tecnologias utilizadas

HTML5 — estrutura do conteúdo

CSS3 — estilização e layout

JavaScript (ES6) — lógica condicional e manipulação do DOM

## 📸 Prévia (exemplo visual)
Caixa Eletrônico
💰 Saldo atual: 500 R$
[Digite o valor do saque]
[ Botão Sacar ]
Mensagem: Saque realizado com sucesso!

## 🎯 Objetivo educacional

Este projeto foi criado para fixar o uso de if e else, aplicando lógica condicional em um caso prático.
Pode ser expandido para incluir novas funcionalidades, como:

Campo de senha de acesso

Extrato de operações na tela

Cálculo das notas entregues

Bloqueio após saldo zerado

## 📄 Licença

Este projeto é de uso livre para fins de estudo.
Sinta-se à vontade para copiar, modificar e aprimorar. 😄